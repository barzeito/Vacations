import "./spinner.css";

function Spinner(): JSX.Element {
    return (
        <div className="spinner">
            <span className="loader"></span>
        </div>
    );
}

export default Spinner;
